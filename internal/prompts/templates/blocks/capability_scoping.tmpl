{{- define "capability-scoping" -}}
{{- if eq .Role "epic-planner"}}

EPIC-WRITING SKILL:
Invoke skill: epic-writing when writing each epic for this task.
{{- else if eq .Role "us-writer"}}

USER-STORY-WRITING SKILL:
Invoke skill: user-story-writing before writing any stories.

CAPABILITY SCOPING:
The parent epic document contains multiple capabilities. Your task covers ONE capability only.
{{- if ne .EpicRef ""}}
The parent epic ref is {{.EpicRef}}. Read {{.Worktree}}/{{.EpicRef}} first; if absent, use `git -C {{.Worktree}} show {{.IntegrationBranch}}:{{.EpicRef}}`.{{- if ne .EpicSection ""}} Then use section #{{.EpicSection}}.{{- end}}
{{- end}}
When reading the epic, use only these sections as input:
- Personas and General Information (shared context for all capabilities)
- Your assigned capability section (match by task SCOPE)
Do NOT read or write stories for other capability sections — those are sibling US Writer tasks.
{{- end}}
{{- end -}}
